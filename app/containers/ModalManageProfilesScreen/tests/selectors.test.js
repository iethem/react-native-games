// import { selectModalManageProfilesScreenDomain } from '../selectors';

describe('selectModalManageProfilesScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
