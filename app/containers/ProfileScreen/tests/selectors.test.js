// import { selectProfileScreenDomain } from '../selectors';

describe('selectProfileScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
