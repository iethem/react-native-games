// import { selectModalAddProfileScreenDomain } from '../selectors';

describe('selectModalAddProfileScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
