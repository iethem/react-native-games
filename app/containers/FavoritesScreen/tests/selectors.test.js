// import { selectFavoritesScreenDomain } from '../selectors';

describe('selectFavoritesScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
