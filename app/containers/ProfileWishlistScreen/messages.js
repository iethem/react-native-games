/*
 * ProfileWishlistScreen Messages
 *
 * This contains all the text for the ProfileWishlistScreen container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProfileWishlistScreen';

export default defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'ProfileWishlistScreen',
  },
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileWishlistScreen container!',
  },
});
