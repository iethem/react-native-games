// import { selectModalVideoScreenDomain } from '../selectors';

describe('selectModalVideoScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
