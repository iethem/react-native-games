// import { selectProfileAppSettingsScreenDomain } from '../selectors';

describe('selectProfileAppSettingsScreenDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
