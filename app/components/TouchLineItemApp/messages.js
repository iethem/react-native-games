/*
 * TouchLineItemApp Messages
 *
 * This contains all the text for the TouchLineItemApp component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TouchLineItemApp';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TouchLineItemApp component!',
  },
});
