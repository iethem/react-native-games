/*
 * GameCardList Messages
 *
 * This contains all the text for the GameCardList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GameCardList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GameCardList component!',
  },
});
