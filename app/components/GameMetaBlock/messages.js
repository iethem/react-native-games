/*
 * GameMetaBlock Messages
 *
 * This contains all the text for the GameMetaBlock component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.GameMetaBlock';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GameMetaBlock component!',
  },
});
