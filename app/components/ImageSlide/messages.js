/*
 * ImageSlide Messages
 *
 * This contains all the text for the ImageSlide component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.ImageSlide';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ImageSlide component!',
  },
});
