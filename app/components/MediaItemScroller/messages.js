/*
 * MediaItemScroller Messages
 *
 * This contains all the text for the MediaItemScroller component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.MediaItemScroller';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MediaItemScroller component!',
  },
});
