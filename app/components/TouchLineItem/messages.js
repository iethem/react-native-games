/*
 * TouchLineItem Messages
 *
 * This contains all the text for the TouchLineItem component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TouchLineItem';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TouchLineItem component!',
  },
});
