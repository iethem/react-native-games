/*
 * TouchLineItemElement Messages
 *
 * This contains all the text for the TouchLineItemElement component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.TouchLineItemElement';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TouchLineItemElement component!',
  },
});
