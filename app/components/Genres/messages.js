/*
 * Genres Messages
 *
 * This contains all the text for the Genres component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.Genres';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Genres component!',
  },
});
