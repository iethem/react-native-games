/*
 * HeaderAccounts Messages
 *
 * This contains all the text for the HeaderAccounts component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderAccounts';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderAccounts component!',
  },
});
